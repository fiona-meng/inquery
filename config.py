model = "gpt-4o-mini"


# BIRD benchmark dataset path (set via env or override here)
BIRD_DATA_PATH = "./bird_data"  # e.g. "/path/to/bird/dev"

# Disable chart and interpret nodes (set True during eval/debugging to save tokens)
DISABLE_CHART_INTERPRET = True
